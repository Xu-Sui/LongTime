#include <time.h>
#include <stdio.h>
#include <stdlib.h>

enum Type {
	INT,
	CHAR,
	FLOAT,
	DOUBLE
};

int SizeType()

void RandomArr(void* arr, int n, size_t size) {

}

void SelectSort(void* arr, int n, size_t size) {

}